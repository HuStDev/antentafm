{
  "name": "antentafm",

  "dockerComposeFile": [
    "${localWorkspaceFolder}/website/docker/docker-compose.rocketchat.yml",
    "${localWorkspaceFolder}/website/docker/docker-compose.node.yml"
  ],

  "service": "nodeserver",

  "workspaceFolder": "/usr/workspace",
  "mounts": [
    "source=${localWorkspaceFolder},target=/usr/workspace,type=bind,consistency=cached"
  ]

  //"forwardPorts": [3000,7000]
}